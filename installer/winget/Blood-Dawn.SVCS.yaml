# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.6.0.schema.json
#
# winget version manifest for SVCS (R3.2a). The triplet
# (version / installer / locale) is what `winget validate` and the
# microsoft/winget-pkgs submission consume. See docs/winget-submission.md.
PackageIdentifier: Blood-Dawn.SVCS
PackageVersion: 2.1.0.dev0
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.6.0
