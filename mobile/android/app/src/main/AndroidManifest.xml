<?xml version="1.0" encoding="utf-8"?>
<!--
  SVCS Mobile - manifest.

  Permissions are deliberately minimal: INTERNET only. The app talks to one
  server the user names and does nothing else. No location, no camera, no
  storage, no contacts. ACCESS_NETWORK_STATE is included only so the pairing
  screen can distinguish "you are offline" from "that address is wrong", which
  is the difference between a user checking their Wi-Fi and a user
  port-forwarding their router in frustration.

  Author: Bloodawn (KheivenD), 2026-07-18 (M1.1).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- M5 slice: a system notification when a server job finishes. Runtime
         permission on Android 13+; declining simply means no notifications. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".SvcsApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.SVCS">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.SVCS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
