# Core vision and video
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Video processing (FFmpeg Python bindings)
ffmpeg-python>=0.2.0

# Pipeline utilities
tqdm>=4.65.0
pyyaml>=6.0
click>=8.1.0

# Metrics and evaluation
scikit-image>=0.21.0    # SSIM, PSNR
scipy>=1.11.0

# Metadata / storage index
# SQLite3 is built into Python stdlib -- no install needed

# Notebooks
jupyter>=1.0.0
matplotlib>=3.7.0
pandas>=2.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Encryption (AES-256-CBC for at-rest segment protection)
cryptography>=42.0

# Optional: lightweight super-resolution (CPU-compatible)
# Uncomment when enhancement module is ready
# basicsr>=1.4.2
# realesrgan>=0.3.0
flask>=3.0
