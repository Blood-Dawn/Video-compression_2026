# tests/security/ - programmatic attack + regression suite for the SVCS security
# audit (docs/SECURITY-AUDIT.md). Each module fires an attack at the app and
# asserts it defends, so a fixed vulnerability cannot silently come back.
